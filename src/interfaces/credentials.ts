export type CARDINAL_CREDENTIALS = {
  endpoint: string;
};

export type CYBERSOURCE_CREDENTIALS = {
  keyId: string;
  secretKey: string;
  merchantId: string;
};

export type KOUNT_CREDENTIALS = {
  merchantID: string;
  ddcUrl: string;
}
